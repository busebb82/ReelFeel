services:
  - type: web
    name: reelfeel
    runtime: docker
    plan: free
    healthCheckPath: /api/genres
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TMDB_API_KEY
        sync: false
